/*Write a program to fill the entire screen with a smiling face. 
The smiling face has an ASCII value 1. */
#include <stdio.h>
int main(){

    for(int smily = 1; smily<=10000;smily++){
        printf("%c ",1);
    }

    return 0;
}